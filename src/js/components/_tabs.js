// const parentElement = document.querySelector('.parent-element'); // Replace 'parent-element' with the actual parent element containing like buttons

// function toggleLike(event) {
//   const target = event.target;

//   if (target.classList.contains('article__wish')) {
//     if (!target.classList.contains('is-liked')) {
//       target.classList.add('is-liked');
//     } else {
//       target.classList.remove('is-liked');
//     }
//   }
// }

// // Add a single click event listener to the parent element
// parentElement.addEventListener('click', toggleLike);

